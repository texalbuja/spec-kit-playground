# Running tests

Run the tests with `pytest` (recommended):

```bash
python3 -m pytest -q
```

Run a single test file:

```bash
python3 -m pytest tests/test_fib.py -q
```

If you prefer the standard library runner, the old `unittest` commands still work.
